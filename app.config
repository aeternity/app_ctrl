%% -*- mode: erlang; erlang-indent-level: 4; indent-tabs-mode: nil -*-

[
  {kernel,
   [
    {logger_level, debug}
,   {logger,
     [
      {handler, app_ctrl, app_ctrl_bootstrap, #{}}
     ]}
   ]}
, {app_ctrl,
   [ {apps, [app_ctrl]} ]
  }
].
